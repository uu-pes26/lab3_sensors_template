{
    "env": {
        "zephyrSourcePath": "${ZEPHYR_BASE}"
    },
    "configurations": [
        {
            "name": "Zepyhr",
            "compileCommands": "${workspaceFolder}/build/compile_commands.json"
        }
    ],
    "version": 4
}
